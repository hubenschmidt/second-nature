"""
Quick Sort

Sort an array of integers in ascending order using the quick sort algorithm.

Constraints:
- 1 <= len(nums) <= 10^5
- -10^4 <= nums[i] <= 10^4
- Must use in-place partitioning (no extra arrays)
- Choose a pivot strategy that avoids worst-case O(n^2) on sorted input

Example:
  Input:  [3, 6, 8, 10, 1, 2, 1]
  Output: [1, 1, 2, 3, 6, 8, 10]

  Input:  [5, -1, 4, 2, -3]
  Output: [-3, -1, 2, 4, 5]
"""


def quick_sort(nums: list[int]) -> list[int]:
    # Implement quick sort here (sort in-place, return nums)
    pass


def partition(nums: list[int], low: int, high: int) -> int:
    # Implement partition and return the pivot index
    pass
